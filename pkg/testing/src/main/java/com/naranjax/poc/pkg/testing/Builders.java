package com.naranjax.poc.pkg.testing;

/**
 * Placeholder entry point for shared test builders.
 *
 * Phase 2 will populate this with fluent builders for common domain objects
 * (e.g. TransactionRiskRequest, RiskDecision) so that unit tests across
 * modules share a single source of default test data.
 */
public final class Builders {

    private Builders() {}

    // TODO(phase-2): add static builder factories for shared domain types
}
