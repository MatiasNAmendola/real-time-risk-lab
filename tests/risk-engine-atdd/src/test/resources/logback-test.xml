<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console: WARN only — keeps test output clean for the summary table -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File: DEBUG — full detail captured for report.sh post-processing -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>build/cucumber.log</file>
        <append>false</append>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{60} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="WARN">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"/>
    </root>

    <logger name="io.riskplatform" level="DEBUG">
        <appender-ref ref="FILE"/>
    </logger>
</configuration>
