# JUnit Jupiter configuration for the integration test module

# Run test classes in parallel (each class gets its own thread, containers are shared statics)
junit.jupiter.execution.parallel.enabled=false

# Display full test names in console output
junit.jupiter.displayname.generator.default=org.junit.jupiter.api.DisplayNameGenerator$ReplaceUnderscores
